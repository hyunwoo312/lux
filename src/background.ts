// MV3 service worker entry, kept wired so the manifest's background reference and the
// unhashed build output stay verified. No background behavior is needed until a feature
// (e.g. the keyboard-shortcut command) requires this runtime context.
export {};
